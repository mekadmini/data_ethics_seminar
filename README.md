```bash
pip install -r requirements.txt
python -m spacy download en_core_web_sm # (ENGLISH as matrix language)
python -m spacy download it_core_news_sm # (ITALIAN as matrix language)
```

```bash
ollama list
ollama pull llama3
ollama pull llama-guard3
```